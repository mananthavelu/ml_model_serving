# Core ML Libraries
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
joblib==1.3.2

# Experiment Tracking
mlflow

# API & Serving
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Data Validation
great-expectations==0.18.8

# Monitoring
prometheus-client==0.19.0
psutil==5.9.6

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1

# Code Quality & Development (allowing pip to manage versions where possible)
flake8
mypy
click
loguru
pytest
pytest-cov
httpx
black
pre-commit==3.5.0 # This one is often good to keep pinned for hook stability

# Data Processing (if specifically needed and causing conflicts)
# scipy==1.11.4 # Unpinning as it's not strictly core and can cause conflicts